export const ADDON_ID = 'storybook-page-builder';
export const TAB_ID = `${ADDON_ID}/tab`;
export const BUILDER_RUNTIME_STORY_ID = `${ADDON_ID}--runtime`;
export const STORAGE_KEY = `${ADDON_ID}/puck-data`;
export const PREVIEW_METADATA_REQUEST = `${ADDON_ID}/preview-metadata-request`;
export const PREVIEW_METADATA_RESPONSE = `${ADDON_ID}/preview-metadata-response`;
export const PREVIEW_METADATA_TIMEOUT_MS = 4000;
